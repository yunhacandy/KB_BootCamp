변수 : 변하는 값을 의미 
        특정 데이터를 담는 메모리(공간)

변수선언 
var a;  => 변수선언 뒤 값을 할당하지 않으면 
           기본적으로 undefined 초기값 가진다.

var b;   //변수 선언
b = 10;  //값 할당 
console.log(b)  => 10

변수의 타입 확인하기  - typeof 

2. String(문자열)
   var a = "hello"; 
   a = 'hi';
   
   자바스트립트는 코드를 실행하는 시점에
   변수의 타입이 결정되는 언어이다. 

 3. Object 
    자바스크립트는 객체기반 언어이다.
    객체는 키(key) - 값(value) 형태로 이루어짐

    let obj = {객체 내용};  => obj변수에 객체를 새로 할당함 

    속성 추가 
    객체를 생성하고 나면 속성을 추가 
    
4. Map객체,

 Set 객체   (순서가 없다. 중복을 허용하지 않는다.)
   
5.Data Type   
   
6. 배열 (Array)
   let array1 = []; //배열리터럴 array1는 배열을 정의


7. 반복문  (Loop)
  - ForEach() 반복문
  - For in  반복문
  - For of  반복문 




   